"""
Command-line entry points for the data pipeline.

Each module here is a standalone step, run as `python -m wavey2.apps.<name>`;
they don't import one another. Shared code belongs in `wavey2` proper.
"""
