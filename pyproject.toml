[project]
name = "wavey2"
version = "0.0.0" # Placeholder; version is tracked in `js/version.js`
requires-python = ">=3.14"
dependencies = [
    "beautifulsoup4",
    "numpy",
    "pygrib",
    "requests",
]

[dependency-groups]
dev = [
    "boto3-stubs[s3]",
    "mypy",
    "ruff",
    "types-requests",
]
archive = [
    "boto3",
]

[tool.mypy]
python_version = "3.14"
strict = true

[[tool.mypy.overrides]]
module = [
    "pygrib",
]
ignore_missing_imports = true

[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I"]
